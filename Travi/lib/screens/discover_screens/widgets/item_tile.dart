/* Author: Trevor Frame
 * Date: 12/07/2020
 * Description: widget for creating a tile for
 * each discovered item. Displays image, title, 
 * description, and publisher information.
 */
import 'package:flutter/material.dart';

class ItemTile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('tile',),
    );
  }
}